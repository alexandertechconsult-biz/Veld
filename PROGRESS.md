# PROGRESS — build ledger

> Single source of truth for loop state. The loop reads this first and writes it last every iteration.
> Status values: `TODO` (default, implied by absence) · `STARTED` · `DONE` · `BLOCKED`.
> A ticket's dependencies must all be `DONE` before the loop will pick it up.

**Project:** Farm Management MVP — offline-first farm record-keeping PWA (see `BACKLOG.md`)
**Stack:** Vite + React + TypeScript, Dexie over IndexedDB, no server, Vercel. Decisions in BACKLOG.md Section 9.
**Ticket order:** the `Seq` column in BACKLOG.md Section 10 is the build order and breaks every priority tie.

## Gate protocol

`GATE-n` rows in the backlog are human checkpoints, not tickets. The loop must never attempt one.
On reaching a gate as the next eligible item, append `BLOCKED: GATE-n — awaiting human verification`
to the Log below and STOP. Barrett clears it by replacing that line with `PASSED: GATE-n @ <timestamp>`.

## Done
<!-- format: - DONE  <TICKET-ID>  <summary>  | tests: <pass/total>  | e2e: <path>  | commit: <hash> -->

- DONE  E0-01  Playwright E2E harness with trace and video recording  | tests: 2/2  | e2e: e2e/recordings/E0-01/20260908T153336Z  | commit: 83f0e37
- DONE  E0-02  Vite + React + TypeScript scaffold with vite-plugin-pwa and Vitest  | tests: 5/5 unit, 2/2 e2e  | e2e: e2e/recordings/E0-02/20260908T162320Z  | commit: eb8cd8c
- DONE  E0-03  Dexie schema + typed data layer  | tests: 23/23 unit  | e2e: e2e/recordings/E0-03/verification.md  | commit: 3ab566b
- DONE  E0-04  App shell + responsive navigation  | tests: 39/39 unit, 8/8 e2e  | e2e: e2e/recordings/E0-04/20260908T164632Z  | commit: e28eb39
- DONE  E0-05  Deploy to Vercel — https://veld-six.vercel.app/  | tests: n/a (infra)  | e2e: e2e/recordings/E0-05/verification.md  | commit: 6550d34
- DONE  GATE-0  Phase 0 human checkpoint — cleared by Barrett, Phase 1 authorised  | evidence: e2e/recordings/E0-05/verification.md + live at https://veld-six.vercel.app/
- DONE  E6-05  Export/import all farm data to a single file  | tests: 52/52 unit, 4/4 e2e  | e2e: e2e/recordings/E6-05/20260908T165655Z  | commit: 04c819b
- DONE  E1-01  Create farm profile (name), persisted in IndexedDB  | tests: 62/62 unit, 2/2 e2e  | e2e: e2e/recordings/E1-01/20260908T200832Z  | commit: b8ced3f
- DONE  E1-02  Add a Livestock or Crop enterprise, each named  | tests: 75/75 unit, 2/2 e2e  | e2e: e2e/recordings/E1-02/20260908T201705Z  | commit: 2d1998f
- DONE  E2-01  Register an animal or group (count 1 = individual, count > 1 = group)  | tests: 95/95 unit, 2/2 e2e  | e2e: e2e/recordings/E2-01/20260908T202435Z  | commit: 92cc5b4
- DONE  E2-02  Log an event (date, type, note) against an animal or group  | tests: 108/108 unit, 2/2 e2e  | e2e: e2e/recordings/E2-02/20260908T203323Z  | commit: 7a981de
- DONE  E2-03  See a history of events per animal or group, most recent first  | tests: 113/113 unit, 2/2 e2e  | e2e: e2e/recordings/E2-03/20260908T203926Z  | commit: 02cd8c3
- DONE  E2-06  Correct or remove a livestock record or event  | tests: 137/137 unit, 2/2 e2e  | e2e: e2e/recordings/E2-06/20260909T081244Z  | commit: ade7f64
- DONE  E3-01  Register a field or block  | tests: 155/155 unit, 2/2 e2e  | e2e: e2e/recordings/E3-01/20260909T081834Z  | commit: 37fcd04
- DONE  E3-02  Log an activity against a field  | tests: 168/168 unit, 2/2 e2e  | e2e: e2e/recordings/E3-02/20260909T083603Z  | commit: b6de327
- DONE  E3-03  See a history of activities per field, most recent first  | tests: 173/173 unit, 2/2 e2e  | e2e: e2e/recordings/E3-03/20260909T084151Z  | commit: f110e78
- DONE  E3-05  Correct or remove a field or activity  | tests: 198/198 unit, 2/2 e2e  | e2e: e2e/recordings/E3-05/20260909T120943Z  | commit: e9da31b
- DONE  E4-01  Create a task  | tests: 221/221 unit, 2/2 e2e  | e2e: e2e/recordings/E4-01/20260909T121650Z  | commit: 23e0301
- DONE  E4-02  Mark a task done  | tests: 228/228 unit, 2/2 e2e  | e2e: e2e/recordings/E4-02/20260909T122304Z  | commit: 58c4aa7
- DONE  E4-03  Edit or delete a task (reopen a done one)  | tests: 250/250 unit, 2/2 e2e  | e2e: e2e/recordings/E4-03/20260909T123117Z  | commit: ebc7a80
- DONE  E5-01  Log a cost or sale  | tests: 271/271 unit, 2/2 e2e  | e2e: e2e/recordings/E5-01/20260909T123840Z  | commit: <pending>

## In progress
<!-- the loop writes STARTED here on entry; should be empty between iterations -->

_Empty._

## Blocked
<!-- format: - BLOCKED  <TICKET-ID>  — <reason>  — <smallest unblocking question> -->

_None._

## Log
<!-- chronological, append-only; STARTED, DONE, BLOCKED and completion markers with timestamps -->

- 2026-09-08 — repo initialised, ledger created.
- 2026-09-08 — stack decisions recorded in BACKLOG.md Section 9; backlog restructured into phases with Seq ordering, explicit dependencies, and gates.
- 2026-09-08 — DONE E0-01. Verified by running `record_e2e.sh E0-01 --ui`: 2 passed, trace.zip and video.webm saved.
- 2026-09-08 — STARTED E0-02.
- 2026-09-08 — DONE E0-02. Vite 7 + React 18 + TS scaffold, vite-plugin-pwa emits `dist/manifest.webmanifest` and `dist/sw.js` on build, Vitest passes 5/5 (formatDocumentTitle + App render), README documents `E2E_DEV_SERVER_COMMAND`. Toolchain moved to Vite 7 / Vitest 3 / vite-plugin-pwa 1 to clear the esbuild dev-server advisory (npm audit: 0 vulnerabilities). E2E boot spec green on mobile and desktop.
- Next eligible: E0-03 (Dexie schema + typed data layer), Seq 3, depends E0-02 which is DONE. (E0-05 also unblocked, Seq 5.)
- 2026-09-08 — STARTED E0-03.
- 2026-09-08 — DONE E0-03. Dexie `VeldDatabase` schema v1 covers all 8 Section-6 entities; generic typed CRUD factory + one DI'd repository per entity behind `src/data/index.ts`; no `any`; foreign-key finders and most-recent-first history. Vitest 23/23 (CRUD per entity + update/delete failure paths + finder isolation + date ordering + close-and-reopen persistence). Typecheck and production build clean; npm audit 0 vulnerabilities. Evidence: e2e/recordings/E0-03/verification.md (infra ticket, no runtime surface). Code review: APPROVE_WITH_NITS (non-atomic update read-then-put noted as acceptable for single-user offline).
- Next eligible: E0-04 (app shell + navigation), Seq 4, depends E0-03 which is now DONE. (E0-05 also unblocked, Seq 5.)
- 2026-09-08 — STARTED E0-04.
- 2026-09-08 — DONE E0-04. Design-system tokens transcribed to `src/styles/tokens.css` (light default; dark under both `prefers-color-scheme` and `[data-theme]`), Inter self-hosted via `@fontsource-variable/inter` (woff2 bundled, works offline), 4/8/12/16/24/32/48 spacing scale. Responsive shell: bottom tab bar below 768px, left sidebar at 768px+ (`src/app/AppShell.tsx`, `Sidebar.tsx`, `BottomTabBar.tsx`); five destinations (Home, Livestock, Crops, Tasks, More→Financials/Settings). Hash router (`useHashRoute.ts`) — no dependency, no server rewrites needed. Every route renders a useful empty state; tap targets 44px min / 56px primary. Colour guard test (`tokens.test.ts`) asserts only palette colours appear in CSS; verified against the built bundle too (13 hexes + one rgba border). Vitest 39/39, Playwright 8/8 on mobile+desktop, typecheck + build clean, npm audit 0 vulnerabilities. Review: APPROVE_WITH_NITS. Evidence: e2e/recordings/E0-04/20260908T164632Z.
- Next eligible: E0-05 (deploy to Vercel), Seq 5, depends E0-02 which is DONE.
- 2026-09-08 — STARTED E0-05.
- 2026-09-08 — BLOCKED E0-05. Deploy requires an authenticated Vercel account. Environment check: `vercel` CLI absent, `VERCEL_TOKEN` unset, no `.vercel`/`vercel.json` in repo, Vercel MCP server unauthorized (needs interactive OAuth, unavailable in this non-interactive loop). Git remote is `github.com/alexandertechconsult-biz/Veld`. None of the acceptance criteria (HTTPS production URL, Android Chrome install prompt, branch preview URLs) can be satisfied without credentials the loop cannot obtain, and faking the URL/`verification.md` is not permitted. No code written; tree clean. See Blocked section for the unblocking options.
- E0-05 blocks GATE-0 (Seq 6) and therefore all of Phase 1. The loop cannot advance past E0-05 until Barrett resolves the Vercel auth question above.
- 2026-09-08 — STARTED E6-05. E0-05 remains human-blocked (no VERCEL_TOKEN, no CLI, no `.vercel` link — re-verified). E6-05 (export/import) depends only on E0-03 (DONE) and is the single eligible buildable P0 ticket, so the loop picks it up out of Seq order per the dependency rule.
- 2026-09-08 — DONE E6-05. Typed backup module `src/data/backup.ts`: `exportData` snapshots all 8 tables in one read transaction into a `{format, version, exportedAt, data}` file; `parseBackup` fully validates untrusted file text (JSON, format tag, version, all 8 tables present as id-bearing arrays) before any write; `importData` clears-then-bulkAdds in one rw transaction so a failure can't half-import. DOM glue and status state isolated in `useBackup` hook (blob download deferred-revoke to avoid the sync-revoke race; `parseBackup` errors surfaced verbatim). SettingsScreen gains a Data backup section with Export/Import buttons and loading/success/error states, alongside the existing empty state (keeps E0-04 spec green). New `.btn-secondary`/`.visually-hidden`/settings CSS uses only design-system tokens (colour-guard test still passes). Vitest 52/52 (13 new: round trip, overwrite-not-merge, empty install, filename, 6 validation-failure cases incl. "does not touch DB when invalid"). Playwright 4/4 on mobile+desktop (import a crafted file → export it back → assert valid downloaded file; plus non-backup file → error alert). Typecheck + build clean, npm audit 0 vulnerabilities. Review: APPROVE_WITH_NITS (revoke-race nit applied). Evidence: e2e/recordings/E6-05/20260908T165655Z.
- Next eligible: none on the critical path — E0-05 (Vercel deploy) stays human-blocked, and it gates GATE-0 and all of Phase 1. Phase-2 tickets E8-01/E8-02 need GATE-1. The loop cannot advance until Barrett resolves the E0-05 Vercel auth question.
- 2026-09-08 — DONE E0-05. Vercel connected via dashboard; verified live with Playwright, 2 passed, manifest and sw.js reachable, cream token intact in built CSS.
- 2026-09-08 — GATE-0 reached. Awaiting human verification on a real device before Phase 1.
- 2026-09-08 — PASSED: GATE-0. Barrett authorised Phase 1. Thirteen tickets now eligible, ending at GATE-1.
- 2026-09-08 — STARTED E1-01 (Seq 7, depends GATE-0 DONE) — first Phase-1 ticket.
- 2026-09-08 — DONE E1-01. Farm-profile create/rename flow in Settings. Domain module `src/data/farmProfile.ts` (`loadCurrentFarm` + `saveFarmProfile`) is dependency-inverted — repositories injected, no DOM, unit-tested against a fresh DB; re-reads the current farm before every save so a repeated save can never create a second farm (MVP is one farm per device). `useFarmProfile` hook wires it to the singleton repositories at the composition root; `FarmProfile.tsx` is a presentational form (Input per design-system §7: 56px, label above, tokens-only CSS so the colour-guard test stays green) that pre-fills the saved name and flips the button Create farm → Save changes. Vitest 62/62 (10 new: 7 domain incl. blank-name-rejected-without-write, trim, rename-not-duplicate, and close-and-reopen persistence; 3 component). Typecheck clean; no ESLint config in project. Review: APPROVE_WITH_NITS (lingering "Farm saved." status is a deferred nit; no observability logging matches the existing useBackup pattern in this client PWA). Evidence: e2e/recordings/E1-01/20260908T200832Z — reload-persistence + rename round-trip, 2/2 on mobile+desktop. (A first record attempt failed with ERR_CONNECTION_REFUSED because E2E_DEV_SERVER_COMMAND was unset; re-run with it set per README, green.)
- Next eligible: E1-02 (add a Livestock/Crop enterprise), Seq 8, depends E1-01 which is now DONE.
- 2026-09-08 — STARTED E1-02 (Seq 8, depends E1-01 DONE).
- 2026-09-08 — DONE E1-02. Add-enterprise flow in Settings. Pure domain module `src/data/enterprises.ts` (`listEnterprises` + `addEnterprise`) is dependency-inverted like E1-01 — repositories injected, no DOM, unit-tested against a fresh DB; always creates (a farm can have many enterprises), requires the farm to exist first (`NoFarmYetError`), trims and rejects a blank name without writing, lists oldest-first. `useEnterprises` hook wires it to the singleton repos at the composition root and returns a success flag so the form clears on add; `Enterprises.tsx` is presentational — Input for the name, a native `<select>` (reuses `.field__input`, keeps its arrow) for the Livestock/Crop type, and a List-row list of existing enterprises with a Beef/Sprout icon and type label. New CSS uses tokens only (colour-guard still green). Vitest 75/75 (13 new: 9 domain incl. blank/no-farm-no-write and close-and-reopen persistence, 4 component). Typecheck + build clean. Review: APPROVE_WITH_NITS (removed a `select` appearance:none that stripped the dropdown arrow; observability deferred, consistent with useFarmProfile/useBackup). Evidence: e2e/recordings/E1-02/20260908T201705Z — create farm → add livestock + crop → reload-persistence, 2/2 on mobile+desktop.
- Next eligible: E2-01 (register an animal or group), Seq 9, depends E1-02 which is now DONE.
- 2026-09-08 — STARTED E2-01 (Seq 9, depends E1-02 DONE).
- 2026-09-08 — DONE E2-01. Register an animal or group in the Livestock module. Pure domain module `src/data/livestock.ts` (dependency-inverted like E1-01/E1-02): `addLivestock` trims name/species and rejects blanks without writing, requires an integer count ≥ 1 (rejects 0 and fractional), and requires the target enterprise to exist AND be a livestock enterprise (a crop enterprise cannot hold animals); `listLivestock` gathers records across every livestock enterprise, oldest first; `listLivestockEnterprises` filters the farm's enterprises. `describeLivestock` is the single place the individual-vs-group rule lives — count 1 → "Individual", count > 1 → "Group of N". `useLivestock` hook wires it to the singleton repos at the composition root and returns a success flag so the form clears. `LivestockScreen` is presentational: loading → empty-state-to-Settings when no livestock enterprise → list + register form (name/tag, species, count; enterprise picker only when more than one livestock enterprise). New `.module`/`.record-*` CSS uses tokens only (colour-guard still green). Vitest 95/95 (20 new: 15 domain incl. both count paths, all validation-failure-no-write cases, cross-enterprise gather, close-and-reopen persistence; 5 component incl. both render paths + disabled-submit + hidden picker). Typecheck + build clean. Review: APPROVE_WITH_NITS (lingering success line + boolean-coercion nit, both consistent with existing patterns and deferred). Evidence: e2e/recordings/E2-01/20260908T202435Z — create farm → livestock enterprise → register individual + group → reload-persistence, 2/2 on mobile+desktop.
- Next eligible: E2-02 (log an event against an animal or group), Seq 10, depends E2-01 which is now DONE.
- 2026-09-08 — STARTED E2-02 (Seq 10, depends E2-01 DONE).
- 2026-09-08 — DONE E2-02. Log an event against an animal or group in the Livestock module. Pure domain module `src/data/events.ts` (dependency-inverted like E2-01): `addEvent` trims the note and rejects blank without writing, requires a finite date and a known type (`EVENT_TYPES` is the single source consumed by both the picker and the validator), and requires the target animal/group to exist; persists through the events repository — straight to IndexedDB, no network call. `listEventsFor` returns one animal's events most-recent-first. `useLivestock` hook extended with a separate `eventStatus`, per-animal `eventCounts`, `logEvent`, and `resetEventStatus` (cleared on form open so a stale error can't render in a fresh form). New presentational `EventForm.tsx` (date defaults to today, type select, note textarea) shown inline under each animal via a "Log event" toggle; row meta gains a "N events" badge. New `.record-row__main`/`.event-form*` CSS uses tokens only (colour-guard still green). Vitest 108/108 (13 new: 10 domain incl. all four types, trim, every failure-without-write path, cross-animal isolation, close-and-reopen persistence; 3 component: log→DB write + badge, disabled-until-note, Cancel-without-write). Typecheck + build clean, npm audit 0 vulnerabilities. Review: APPROVE_WITH_NITS (stale-event-status nit fixed in-review; N-query count load and no observability both deferred, consistent with prior tickets). Evidence: e2e/recordings/E2-02/20260908T203323Z — create farm → livestock enterprise → register animal → log event → reload-persistence, 2/2 on mobile+desktop.
- Next eligible: E2-03 (see a history of events per animal or group), Seq 11, depends E2-02 which is now DONE.
- 2026-09-08 — STARTED E2-03 (Seq 11, depends E2-02 DONE).
- Next eligible after E2-03: E2-06 (correct or remove a livestock record or event), Seq 12, depends E2-03 which is now DONE.
- 2026-09-09 — STARTED E2-06 (Seq 12, depends E2-03 DONE).
- 2026-09-09 — DONE E2-06. Correct or remove a livestock record or event. Domain: `updateLivestock`/`deleteLivestock` and `updateEvent`/`deleteEvent` added to the pure, dependency-inverted `data/livestock` and `data/events` modules. Update takes a partial patch and validates only the fields supplied, leaving the rest untouched — so an event keeps its original date unless the date is the thing being corrected (covered by a dedicated test). `deleteLivestock` cascades: it removes the record's events too, so no orphans are left; both deletes are idempotent. New `LivestockNotFoundError`/`EventNotFoundError` for missing-target edits. `useLivestock` gains `editAnimal`/`removeAnimal`/`editEvent`/`removeEvent` (each reloads via a shared `reload` helper; known errors mapped to farmer-facing messages); the saved status now carries its own message so edit/delete never show the register confirmation. UI: new presentational `LivestockRow`, `LivestockEditForm`, `EventRow`, and a reusable inline `ConfirmDelete` (first tap names exactly what will be removed, second tap deletes — inline, not a modal, per the design-system rule); `EventForm` generalised to also edit (pre-filled, `submitLabel`/`title`). Destructive buttons use the `--danger` token as a secondary-button modifier; colour-guard still green. Vitest 137/137 (24 new: domain update/delete for both entities incl. date-preservation, cascade delete, all validation-without-write and not-found paths, close-and-reopen persistence; component edit/delete incl. named confirmations and "nothing removed until confirmed"). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors; deferred: shared event-status flashes "Saving…" on both forms briefly, no observability layer — consistent with prior tickets). Evidence: e2e/recordings/E2-06/20260909T081244Z — edit animal → edit event (ISO date preserved) → reload persists → delete event (named confirm) → delete animal (named confirm), 2/2 on mobile+desktop. (A first recording failed on a locale-dependent date assertion in the spec; fixed to check the ISO date-input value and re-recorded green — product was never at fault.)
- Note: `npm audit` now reports 2 moderate advisories in `@vitest/mocker` (dev-only test tooling, not in the shipped bundle), newly published since E2-03; package files unchanged by this ticket and the fix is a breaking `vitest` major bump — out of scope, left for a maintenance ticket.
- Next eligible: E3-01 (register a field or block), Seq 13, depends E1-02 which is DONE.
- 2026-09-09 — STARTED E3-01 (Seq 13, depends E1-02 DONE) — first Crops-module ticket.
- 2026-09-09 — DONE E3-01. Register a field or block in the Crops module — the crop analogue of E2-01, register-only (activity logging E3-02, history E3-03, edit/delete E3-05 stay separate tickets). Pure dependency-inverted domain module `src/data/fields.ts`: `addField` trims name/cropType and rejects a blank one without writing, trims the optional size and omits it when blank (never stores `""`), and requires the target enterprise to exist AND be a crop enterprise (a livestock enterprise cannot hold fields); `listFields` gathers across every crop enterprise oldest-first; `listCropEnterprises` filters the farm's enterprises. `useFields` hook wires it to the singleton repos at the composition root and returns a success flag so the form clears. `CropsScreen` replaces its empty state with: loading → empty-state-to-Settings when no crop enterprise → field list + register form (name, crop type, optional size; enterprise picker only when more than one crop enterprise), reusing the existing `.module`/`.record-row` CSS (no new colours, colour-guard still green). Vitest 155/155 (18 new: 12 domain incl. optional-size present/absent/blank-omitted, all validation-without-write paths, livestock-enterprise refusal, cross-enterprise gather, close-and-reopen persistence; 6 component incl. both render paths, disabled-submit, picker visibility). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors; deferred boolean-coercion in canSubmit and no observability layer, both consistent with prior tickets). Evidence: e2e/recordings/E3-01/20260909T081834Z — create farm → crop enterprise → register field with size + field without size → reload persists both, 2/2 on mobile+desktop.
- Next eligible: E3-02 (log an activity against a field), Seq 14, depends E3-01 which is now DONE.
- 2026-09-09 — STARTED E3-02 (Seq 14, depends E3-01 DONE).
- 2026-09-09 — DONE E3-02. Log an activity against a field — the crop analogue of E2-02, log-only (history E3-03 and edit/delete E3-05 stay separate). Pure dependency-inverted domain module `src/data/activities.ts`: `ACTIVITY_TYPES` (planting/input/harvest/other) is the single source consumed by both picker and validator; `addActivity` trims the note and rejects blank without writing, requires a finite date and a known type, and requires the target field to exist (`NoFieldError`) — persisted through the activities repository straight to IndexedDB, no network call. `listActivitiesFor` returns one field's activities most-recent-first. `useFields` hook extended with a separate `activityStatus`, per-field `activityCounts`, `logActivity`, and `resetActivityStatus` (cleared on form open so a stale error can't render in a fresh form). New presentational `ActivityForm.tsx` (date defaults to today, type select, note textarea) and `FieldRow.tsx` (always-visible line + "N activities" badge, a "Log activity" toggle revealing the form); `CropsScreen` now renders fields via `FieldRow`. Reuses existing `.record-row`/`.event-form` CSS — no new colours, colour-guard still green. Vitest 168/168 (13 new: 10 domain incl. all four types, trim, every failure-without-write path, cross-field isolation + most-recent-first, close-and-reopen persistence; 3 component: log→DB write + count badge, disabled-until-note, cancel-writes-nothing). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors). Evidence: e2e/recordings/E3-02/20260909T083603Z — farm → crop enterprise → field → log activity → count badge → reload persists, 2/2 on mobile+desktop.
- Note: fixed a pre-existing suite flake surfaced by the added tests. `listFields`/`listEnterprises`/`listLivestock` sort "oldest first" by `createdAt`, but records created within the same millisecond tied on `createdAt` and fell back to random UUID key order — intermittently reddening `fields.test.ts` and the livestock delete empty-state assertion. Root-caused in the one place the layer reads the clock: `data/ids.ts` `now()` is now strictly monotonic (same-ms inserts get increasing timestamps), and the livestock delete test's final empty-state assertion switched from `getByText` to `findByText` to await the reload re-render. Suite is deterministic across 37+ consecutive runs. All `entities.test.ts` timestamp assertions still hold.
- Next eligible: E3-03 (see a history of activities per field), Seq 15, depends E3-02 which is now DONE.
- 2026-09-09 — STARTED E3-03 (Seq 15, depends E3-02 DONE).
- 2026-09-09 — DONE E3-03. See a history of activities per field or block in the Crops module — the crop analogue of E2-03, read-only (edit/delete is E3-05). The chronological ordering already lived in the data layer (`listActivitiesFor` → most recent first) from E3-02; this ticket surfaces it. `useFields` now stores each field's full activity array (`activitiesByField`) instead of only a count — `loadActivities` replaces `loadActivityCounts`, so the history and the row's "N activities" badge derive from one source (no second query, no discarded data). New presentational `ActivityHistory.tsx` renders the activities it is given, in order, as an ordered list (type label from the single ACTIVITY_TYPES source, human date via `toLocaleDateString`, note), with a "No activities logged yet." empty state; it does no sorting or persistence of its own and carries no edit/delete controls (those are E3-05). `FieldRow` now takes the `history` array (badge = `history.length`) and renders `ActivityHistory` above the log form in the expanded panel; `CropsScreen` passes `activitiesByField[field.id] ?? []`. Reuses the existing `.record-row__panel`/`.event-history*` CSS from E2-03 — no new colours, colour-guard still green. Vitest 173/173 (5 new: 3 ActivityHistory — empty, render type+note, order-preserved; 2 CropsScreen — seeded out-of-order renders newest-first + count badge, and log-then-appears-in-history with the empty-state assertion). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors; deferred: unreachable invalid-date `<time>` guard and no observability layer, both consistent with E2-03/prior tickets). Evidence: e2e/recordings/E3-03/20260909T084151Z — farm → crop enterprise → field → log older + newer activity → history lists newest-first → reload persists order, 2/2 on mobile+desktop.
- Next eligible: E3-05 (correct or remove a field or activity), Seq 16, depends E3-03 which is now DONE.
- 2026-09-09 — STARTED E3-05 (Seq 16, depends E3-03 DONE).
- 2026-09-09 — DONE E3-05. Correct or remove a field or activity — the crop analogue of E2-06, matching it feature-for-feature. Domain: `updateField`/`deleteField` and `updateActivity`/`deleteActivity` added to the pure, dependency-inverted `data/fields` and `data/activities` modules. Update takes a partial patch and validates only the fields supplied, leaving the rest untouched — so an activity keeps its original date unless the date is the thing being corrected (dedicated test); a supplied-but-blank field size clears the optional size rather than storing "". `deleteField` cascades: it removes the field's activities too, so no orphans are left; both deletes are idempotent. New `FieldNotFoundError`/`ActivityNotFoundError` for missing-target edits. `useFields` gains `editField`/`removeField`/`editActivity`/`removeActivity` (each reloads via the shared `reload` helper; known errors mapped to farmer-facing messages); the field status carries its own message so edit/delete never show the register confirmation. UI: new presentational `FieldEditForm` and `ActivityRow` (the crop twins of `LivestockEditForm`/`EventRow`), reusing the existing inline `ConfirmDelete` (first tap names exactly what will be removed, second tap deletes — inline, not a modal); `ActivityForm` generalised to also edit (pre-filled, `submitLabel`/`title`); `ActivityHistory` now renders `ActivityRow` with edit/delete; `FieldRow` gains the edit-field/delete-field tools. No new colours — all reuse the E2-06 `record-row__tools`/`confirm-delete`/`btn-secondary--danger` classes, colour-guard still green. Vitest 198/198 (25 new: domain update/delete for both entities incl. date-preservation, size-clear, cascade delete, all validation-without-write and not-found paths, close-and-reopen persistence; component edit/delete incl. named confirmations and "nothing removed until confirmed"). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors; deferred: size ternary style and no observability layer — consistent with prior tickets). Evidence: e2e/recordings/E3-05/20260909T120943Z — edit field → edit activity (ISO date preserved) → reload persists → delete activity (named confirm) → delete field (named confirm), 2/2 on mobile+desktop.
- Note: `npm audit` still reports the 2 moderate `@vitest/mocker` advisories (dev-only test tooling, not in the shipped bundle); package files unchanged by this ticket, fix is a breaking `vitest` major — out of scope, left for a maintenance ticket.
- Next eligible: E4-01 (create a task), Seq 17, depends E1-01 which is DONE — first Tasks-module ticket.
- 2026-09-09 — STARTED E4-01 (Seq 17, depends E1-01 DONE) — first Tasks-module ticket.
- 2026-09-09 — DONE E4-01. Create a task in the Tasks module. Pure dependency-inverted domain module `src/data/tasks.ts`: `addTask` trims the title and rejects a blank one without writing, requires the farm to exist first (`NoFarmForTaskError`), and treats link/assignee/due-date as optional — a supplied field/animal link is validated to still exist (`TaskLinkNotFoundError`), a blank assignee is omitted rather than stored as "", and a supplied due date must be a finite point in time (`InvalidDueDateError`). A new task always starts `open` (mark-done E4-02 and edit/delete E4-03 stay separate). `listTasks` returns the farm's tasks most-recently-created first; `listTaskLinkOptions` gathers the farm's fields then animals as already-labelled picker options (empty when none, so the UI omits the picker). `useTasks` hook wires it to the singleton repos at the composition root and returns a success flag so the form clears; the screen distinguishes a `no-farm` state from an empty list. `TasksScreen` replaces its empty state with: loading → empty-state-to-Settings when no farm → task list + create form (title, optional link picker, assignee, due date), reusing existing `.module`/`.record-row`/`.farm-form`/`.field` CSS (no new colours, colour-guard still green). Vitest 221/221 (23 new: 17 domain incl. every failure-without-write path, both link types + non-existent-link refusal, assignee trim/omit, due-date store/reject, most-recent-first, empty-when-no-farm, close-and-reopen persistence, link-options ordering; 6 component: no-farm empty state, create-from-title, disabled-until-title, assignee+due capture/display, field-link via picker, picker-hidden-when-no-links). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors; deferred observability layer, consistent with prior tickets). Evidence: e2e/recordings/E4-01/20260909T121650Z — farm → crop enterprise → field → create task with field link + assignee + due date → title-only task → reload persists both, 2/2 on mobile+desktop.
- Next eligible: E4-02 (mark a task done), Seq 18, depends E4-01 which is now DONE.
- 2026-09-09 — STARTED E4-02 (Seq 18, depends E4-01 DONE).
- 2026-09-09 — DONE E4-02. Mark a task done in a single tap. Domain: `markTaskDone` added to the pure, dependency-inverted `data/tasks` module — flips a task's status to `done` in one call, requires the task to exist (`TaskNotFoundError`), and is idempotent (marking an already-done task done again is a harmless no-op). Reopening/edit/delete stay in E4-03. `useTasks` gains `markDone` (reloads the list, known error mapped to a farmer-facing message, `busy` guards the control during the write). UI: the task list now renders grouped into Open and Done (BACKLOG §8.2) via a new presentational `TaskRow`; open rows carry a single-tap circle control (`Mark "<title>" done`, 44px target) and done rows show a completed check (`--primary` for strong contrast in both themes) with a line-through title. New `.task-group__title`/`.task-row__*` CSS uses tokens only (colour-guard still green). Vitest 228/228 (5 new domain: flip-to-done, persistence, idempotent, other-fields-untouched, not-found-without-write; 2 new component: single-tap moves Open→Done and drops the control, seeded open/done stay in separate groups; E4-01 tests updated for the renamed `Open tasks` list label). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors; deferred: per-render filter and no observability layer, both consistent with prior tickets). Evidence: e2e/recordings/E4-02/20260909T122304Z — farm → two tasks → mark one done in one tap → moves to Done, other stays Open, control gone → reload persists done, 2/2 on mobile+desktop.
- Next eligible: E4-03 (edit or delete a task), Seq 19, depends E4-02 which is now DONE.
- 2026-09-09 — STARTED E4-03 (Seq 19, depends E4-02 DONE).
- 2026-09-09 — DONE E4-03. Edit or delete a task, and reopen a done one — the Tasks analogue of E2-06/E3-05. Domain: `updateTask`/`deleteTask`/`reopenTask` added to the pure, dependency-inverted `data/tasks` module. `updateTask` takes a full editable snapshot (`TaskEdit`: title, link, assignee, due date) rather than a partial patch — the edit form always supplies every editable field — so an omitted field/animal link, a blank assignee, or an omitted due date clears that field, while the title is trimmed and a blank one is rejected without writing, a supplied link is validated to still exist, and a supplied due date must be finite; the task's done/open status is left untouched. `reopenTask` flips a done task back to `open` (idempotent, mirrors `markTaskDone`); `deleteTask` is idempotent with nothing to cascade. All three reuse the existing `TaskNotFoundError`. `useTasks` gains `editTask`/`removeTask`/`reopen`, each reloading via a shared `reload` helper with known errors mapped to farmer-facing messages and its own saved message. UI: new presentational `TaskEditForm` (pre-filled title/link/assignee/due date, clearing a field clears it on save) and a `record-row__tools` row on every `TaskRow` — Edit (inline form), Delete (reuses the naming `ConfirmDelete`), and Reopen on done rows. The `field:id`/`livestock:id` link encoding was extracted from `TasksScreen` into shared `screens/taskLinks.ts` (NO_LINK/encodeLink/decodeLink + `currentLinkValue`) so create and edit forms share one source. No new colours — reuses E2-06 `record-row__tools`/`event-form`/`confirm-delete`/`btn-secondary--danger`, colour-guard still green. Vitest 250/250 (22 new: domain update change/clear/validate-without-write for title/link/assignee/due-date, status-untouched-on-edit, reopen flip/persist/idempotent/not-found, delete remove/idempotent/only-named, cleared-fields-persist-across-reopen; component inline-edit persists+closes, delete-needs-named-confirmation, reopen moves Done→Open). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors; deferred shared-status flash on the edit form and no observability layer, both consistent with prior tickets). Evidence: e2e/recordings/E4-03/20260909T123117Z — edit title inline → mark done → reopen → delete behind named confirm → reload persists edit and delete, 2/2 on mobile+desktop.
- Next eligible: E5-01 (log a cost or sale), Seq 20, depends E1-02 which is DONE — first Financials-module ticket.
- 2026-09-09 — STARTED E5-01 (Seq 20, depends E1-02 DONE) — first Financials-module ticket.
- 2026-09-09 — DONE E5-01. Log a cost or sale in the Financials module — create-only (running total E5-02 and edit/delete E5-03 are separate, gated Phase-3 tickets). Pure dependency-inverted domain module `src/data/transactions.ts`: `TRANSACTION_TYPES` (cost/sale) is the single source consumed by both the type picker and the validator; `addTransaction` requires the farm to exist first (`NoFarmForTransactionError`), requires a positive finite amount (`InvalidAmountError`, rejects 0/negative/NaN) and a finite date (`InvalidTransactionDateError`), validates a supplied enterprise link still exists (`TransactionEnterpriseNotFoundError` — omit for farm-level overhead), and trims/omits a blank note rather than storing "" — all rejections write nothing; persisted through the transactions repository straight to IndexedDB, no network call. `listTransactions` returns the farm's transactions most-recent-first by date, ties broken by creation order so the list is deterministic. `useTransactions` hook wires it to the singleton repos at the composition root, loads the enterprises the picker offers via `listEnterprises`, distinguishes a `no-farm` state, and returns a success flag so the form clears. `FinancialsScreen` replaces its empty state with: loading → empty-state-to-Settings when no farm → running list + log form (type, amount, date, optional enterprise picker shown only when enterprises exist, optional note), reusing existing `.module`/`.record-row`/`.farm-form`/`.field`/`.event-form__note` CSS (no new colours, colour-guard still green). Vitest 271/271 (16 new domain: cost + sale, optional link/note present/trimmed/blank-omitted, every failure-without-write path incl. unknown type, most-recent-first + same-date tiebreak, close-and-reopen persistence; 5 new component: no-farm empty state, log-and-list-then-clear, disabled-until-positive-amount, sale+enterprise+note round-trip to DB, picker-hidden-when-no-enterprises). Typecheck + build clean. Review: APPROVE_WITH_NITS (no blockers/majors; deferred: check-order judgment call and no observability layer, both consistent with prior tickets). Evidence: e2e/recordings/E5-01/20260909T123840Z — farm → livestock enterprise → log a cost → log a later sale linked to the enterprise → newest-first ordering → reload persists both, 2/2 on mobile+desktop.
- Note: `npm audit` still reports the 2 moderate `@vitest/mocker` advisories (dev-only test tooling, not in the shipped bundle); package files unchanged by this ticket, fix is a breaking `vitest` major — out of scope, left for a maintenance ticket.
- Next eligible: E7-01 (home screen showing recent activity across all modules), Seq 21, depends E2-02, E3-02, E4-01, E5-01 — all now DONE.
- 2026-09-08 — DONE E2-03. See a history of events per animal or group in the Livestock module. The chronological ordering already lived in the data layer (`listEventsFor` → `mostRecentFirst`, most recent first) from E2-02; this ticket surfaces it. `useLivestock` now stores each animal's full event array (`eventsByAnimal`) instead of only a count — `loadEvents` replaces `loadEventCounts`, so the history and the row's "N events" badge derive from one source (no second query, no discarded data). New presentational `EventHistory.tsx` renders the events it is given, in order, as an ordered list (type label from the single EVENT_TYPES source, human date via `toLocaleDateString`, note), with a "No events logged yet." empty state; it does no sorting or persistence of its own. Shown inside the expanded row above the log-event form via a new `.record-row__panel`. New `.event-history*` CSS uses only design-system tokens (`--surface-sunk`, `--radius`, spacing, text/label sizes) so the colour-guard test stays green. Vitest 113/113 (5 new: 3 EventHistory — empty, render type+note, order-preserved; 2 LivestockScreen — seeded out-of-order renders newest-first + badge, and log-then-appears-in-history with the empty-state assertion). Typecheck + build clean. Review: APPROVE_WITH_NITS (unreachable invalid-date `<time>` guard and the pre-existing N-query event load both deferred; no observability layer in this client PWA, consistent with prior tickets). Evidence: e2e/recordings/E2-03/20260908T203926Z — farm → livestock enterprise → animal → log older + newer event → history lists newest-first → reload persists order, 2/2 on mobile+desktop.
