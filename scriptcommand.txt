# Run the RALPH loop (macOS / zsh). Run from the worktree root, not your main checkout.
bash .claude/skills/ralph-loop/scripts/ralph.sh \
  --max-iterations 5 \
  --branch ralph/phase-0 \
  --prompt .claude/skills/ralph-loop/references/PROMPT.md \
  --progress ./PROGRESS.md

# Halt cleanly between iterations:  touch STOP
# The script deletes STOP on startup, so no need to remove it before the next run.
